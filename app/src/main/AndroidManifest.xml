<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pcs.ub.edu.ar.clinicavirtual">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitys.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.GoogleSignInActivity" />
        <activity android:name=".activitys.DataRegisterActivity" />
        <activity android:name=".activitys.ProfileActivity" />
        <activity android:name=".activitys.SearchTurnActivity" />
        <activity android:name=".activitys.MyTurnsActivity" />
        <activity android:name=".activitys.TurnDataActivity" />
        <activity
            android:name=".activitys.MainScreenActivity"
            android:label="@string/title_activity_main_screen"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>